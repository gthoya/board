<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="com.oopstudy.board.test">
    <select id="selectTest" resultType="com.oopstudy.board.test.entity.Test">
        SELECT id, name
        FROM test
        WHERE id = #{id}
    </select>
</mapper>